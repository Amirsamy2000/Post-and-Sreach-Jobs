@model IEnumerable<Employement_Project_MVC.Models.ApplayForJob>@if (Model.Count() != 0){<div class="panel panel-heading" style="color:gray">    <h2> المتقدمين علي وظيفة :@Model.ToList()[0].job.JobTtile </h2>    <div class="panel panel-body">        <p>@Model.ToList()[0].job.SreachEngigJob</p>        <h4>عدد المتقدمين :@Model.Count()</h4>    </div></div><div class="panel panel-body">    <table id="customers">        <tr>            <th>                @Html.DisplayName("الاسم")            </th>            <th>                @Html.DisplayName("البلد")            </th>            <th>                @Html.DisplayNameFor(model => model.Message)            </th>            <th>                @Html.DisplayNameFor(model => model.AplayOn)            </th>            <th>                @Html.DisplayNameFor(model => model.CvApplay)            </th>            <th>                متابعة            </th>        </tr>        @foreach (var item in Model)        {<tr>    <td>@Html.DisplayFor(modelItem => item.user.UserName)</td>    <td>@Html.DisplayFor(modelItem => item.user.Country)</td>    <td>@Html.Raw(HttpUtility.HtmlDecode(Html.DisplayFor(modelItem => item.Message).ToString()))</td>    <td>@Html.DisplayFor(modelItem => item.AplayOn)</td>    <td>        <a class="btn  btn-primary btn-block btncv" href="~/CVS/@item.CvApplay" target="_blank">رؤية</a>        <a class="btn btn-primary btn-block btncv" href="~/CVS/@item.CvApplay" download="download">تنزيل </a>    </td>    <td>        @if (item.Accept == true)        {    <p style="float:right ;margin-right:5px;background-color:forestgreen;opacity:0.8;color:white; padding:10px;min-width:200px">تم قبول @item.user.UserName للوظيفة</p>    <a class="btn btn-info" href="whatsapp://send?phone=+200112873492" target="_blank" style="float:left ">        تواصل واتساب    </a>}else{    @Ajax.ActionLink("قبول", "Applies_Profile", new { JobId = item.JobId, accpet = item.Id }, new AjaxOptions    {        HttpMethod = "POST",        InsertionMode = InsertionMode.Replace,        UpdateTargetId = "Applies",        OnBegin = "CancelResult",        OnSuccess = "onSuccessFunction",    }, new { @class = "btn btn-success " });    @Ajax.ActionLink("رفض", "Applies_Profile", new { id = item.Id, JobId = item.JobId }, new AjaxOptions    {        HttpMethod = "POST",        InsertionMode = InsertionMode.Replace,        UpdateTargetId = "Applies",        OnBegin = "CancelResult",        OnSuccess = "onSuccessFunction",    }, new { @class = "btn btn-danger" });    <a class="btn btn-warning reject" href="@item.LinksGlary" target="_blank">معرض الاعمال</a>    <a class="btn btn-info">تواصل</a>}    </td></tr>}    </table></div>}else{<div class="panel panel-body">    <center>    </center></div><div class="panel panel-default" style="background-color: gray;">        <center>        <div class="panel-body" style="color:white">        لا توجد اي من متقدمين علي الوظيفة</div>    </center></div>}<script>    //var accpt = document.getElementsByClassName("accpt")[0];    //accpt.onclick ==function () {    //    document.getElementsByClassName("reject")[0].style.display = "none";    //    document.getElementsByClassName("reject")[1].style.display = "none";    //    document.getElementById("accpted_div").style.display = "";    //};    function ClearResult() {        document.getElementById("EmployeeDiv").innerHTML = ""    }    function succ() {        window.scrollBy(0, document.body.scrollHeight)        document.getElementById("LoadingApplies").style.display = "none"    }    function CancelResult() {    }</script>